[project]
name = "upliftpolicy"
version = "0.1.0"
description = "Retention targeting by uplift with doubly-robust policy evaluation, including the customers your outreach drives away."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
